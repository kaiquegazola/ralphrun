dist/
node_modules/
coverage/

# desktop app build artifacts (app/) — the Hutch devkit and toolchain are
# downloaded per machine, and run-child.js is generated by `bun run build:child`
app/.cottontail-tmp/
app/.hutch/
app/artifacts/
app/build/
app/resources/
*.log
.DS_Store

# local run files (never commit user backlogs)
prd.json
ralph.config.json
progress.md
